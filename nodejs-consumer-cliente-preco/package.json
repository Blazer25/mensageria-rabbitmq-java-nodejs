{
  "name": "nodejs-consumer-cliente-preco",
  "version": "1.0.0",
  "description": "Consumidor RabbitMQ referente a parte de preços da entidade cliente.",
  "scripts": {
    "build": "tsc Consumidor.ts",
    "dev": "nodemon --exec ts-node Consumidor.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Blazer25",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.4"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/nodemon": "^1.19.6"
  }
}
